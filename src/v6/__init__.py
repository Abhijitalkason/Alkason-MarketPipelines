"""PLAN v6 swing (1-5 day, long-only, CNC delivery) selective signal system.

Reuses the v3/v4 config and IST clock; everything in src/v6/ reads the `v6:`
section of config_v3.yaml via src.intraday.load_config.
"""
