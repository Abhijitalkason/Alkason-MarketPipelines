# ── PLAN v3 — Intraday Selective Signal System ────────────────────────
# Full dev/training/serving stack. The API container uses the lean subset in
# docker/requirements-api.txt. mapie is intentionally absent — the conformal
# gate is a custom ACI loop (recorded deviation, v3_supplementry §17).

# ── Data Processing ───────────────────────────────────────────────────
pandas>=2.2,<3
numpy>=1.26,<3
scipy>=1.14.0
pyarrow>=16.0

# ── Models ────────────────────────────────────────────────────────────
lightgbm>=4.5
catboost>=1.2.7
scikit-learn>=1.6,<1.8

# ── Hyperparameter optimisation ───────────────────────────────────────
optuna>=4.0

# ── Calibration / explainability ──────────────────────────────────────
shap>=0.45

# ── Experiment tracking + versioning ──────────────────────────────────
mlflow>=3.0
dvc>=3.50

# ── Drift monitoring ──────────────────────────────────────────────────
evidently>=0.7.0

# ── SLM explanation (Ollama client; Granite runs in the ollama service) ─
ollama>=0.6.2

# ── API framework ─────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.5

# ── Visualisation (backtest frontier/reliability artifacts) ───────────
matplotlib>=3.7

# ── Config & utilities ────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.30.0
tqdm>=4.60.0

# ── Testing ───────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
