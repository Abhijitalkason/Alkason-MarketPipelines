# ── v3 API container dependencies (inference + serving only) ──────────────────
# No FinBERT, chronos, autogluon, optuna, xgboost, torch, news scraping —
# the v3 serving path needs none of them.

# ── Data Processing ───────────────────────────────────────────────────────────
pandas>=2.2,<3
numpy>=1.26,<3
scipy>=1.14.0
pyarrow>=16.0

# ── Models (inference) ────────────────────────────────────────────────────────
lightgbm>=4.5
catboost>=1.2.7
scikit-learn>=1.6,<1.8

# ── Calibration / explainability ──────────────────────────────────────────────
shap>=0.45

# ── Experiment tracking (registry load at startup) ────────────────────────────
mlflow-skinny>=3.0

# ── Drift ─────────────────────────────────────────────────────────────────────
evidently>=0.7.0

# ── SLM explanation (Ollama client; Granite runs in the ollama service) ───────
ollama>=0.6.2

# ── API framework ─────────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.5

# ── Config & utilities ────────────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.30.0
